{
  "name": "node-red-contrib-stopwatch",
  "version": "0.0.1",
  "description": "A simple stopwatch for very basic diagnostics purposes. Uses NodeJS's `process.hrtime()`, so take the results with a grain of salt.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node-red",
    "stopwatch"
  ],
  "author": "ContainedBlargh",
  "license": "ISC",
  "dependencies": {},
  "node-red": {
    "nodes": {
      "stopwatch": "stopwatch.js"
    }
  }
}
